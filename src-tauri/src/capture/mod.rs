/// The `capture` module handles all media capture for beme.
/// Currently only screen capture is implemented; audio capture will follow
/// once the `cpal` dependency is added.
pub mod screen;

pub mod audio;
